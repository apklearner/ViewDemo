<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.viewdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.stepdetector"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CheckPkgActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".acts.BehindViewActivity"
            android:theme="@style/ChildTheme"

            />
        <activity
            android:name=".acts.AutoFlowTextActivity"
            android:theme="@style/ChildTheme" />

        <activity
            android:name=".acts.DragViewTestActivity"
            android:theme="@style/ChildTheme" />
        <activity
            android:name=".acts.SlideMenuActivity"
            android:theme="@style/ChildTheme" />
        <activity
            android:name=".acts.FloatActivity"
            android:theme="@style/ChildTheme" />
        <activity android:name=".StepActivity" />
        <activity android:name=".UseageTimeActivity" />

        <activity android:name=".WebActivity" />
    </application>

</manifest>